---
"@mikenotthepope/substrateui": minor
---

Add `info` variants to the components that consume status colors: Alert (`variant="info"`), Badge (`variant="info"`), and Toaster (`toast.info()` toasts are now styled with the info status tokens instead of falling back to default styling).
