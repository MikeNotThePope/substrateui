---
"@mikenotthepope/substrateui": patch
---

Remove unused `date-fns` dependency, re-export `useDirection` directly from Radix, and simplify the library build config (esbuild resolves `@/` via tsconfig paths natively).
